package com.revature.actors;

public class Teller {
	/*
	 * 
	 * open()
	 * 	bool continue = true
	 * 	do
	 * 		"welcome"
	 * 		continue = loginmenu.navigate();
	 * 		if(continue)
	 * 			do
	 * 			continue = accountMenu.navigate();
	 * 			if(continue)
	 * 				continue = transactionMenu.navigate();
	 * 			endif
	 * 			while (continue)
	 * 		endif
	 * 
	 * 		"do you wish to start a new session?"
	 * 	while(continue);
	 * 
	 * 
	 */
}
